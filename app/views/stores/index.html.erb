<!-- Validation failure messages -->
<% if @store.errors.any? %>
  <% @store.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h1>
    Manage Stores
    <!-- <a href="/stores/new" class="btn btn-lg btn-success">New Store</a> -->
  </h1>
</div>

<br>

<!-- <div class="row">
  <div class="col-sm-3">Store Name</div>
  <div class="col-sm-6">
    input field here
  </div>
  <div class="col-sm-3">
    button here
  </div>
</div> -->

<div class="row">
  <div class="col-md-12">
    <form action="/create_store" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          New Store Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control">
      </div>

      <button class="btn btn-success">
        Add New Store
      </button>
    </form>
  </div>
</div>


<br>
<br>


<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Current List of Stores</th>
        <th>Actions</th>
      </tr>

      <% @stores.each do |store| %>
      <tr>
        <td><%= store.name %></td>
        <td>
          <!-- <a href="/stores/<%= store.id %>" class="btn btn-primary">Show</a> -->
          <a href="/stores/<%= store.id %>/edit" class="btn btn-warning">Edit</a>
          <!-- <a href="/delete_store/<%= store.id %>" class="btn btn-danger" rel="nofollow">Delete</a> -->
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
