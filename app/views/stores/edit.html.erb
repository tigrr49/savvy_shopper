<!-- Validation failure messages -->
<% if @store_to_update.errors.any? %>
  <% @store_to_update.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit <%= @store.name %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/update_store/<%= @store.id %>" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          New Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @store_to_update.name %>">
      </div>

      <button class="btn btn-warning">
        Update Store
      </button>
      or
      <a href="/stores/">Cancel</a>
    </form>
  </div>
</div>
